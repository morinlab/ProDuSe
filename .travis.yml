language: python
python:
    - "3.3"
    - "3.4"
    - "3.5"
    - "3.6"

before_install:
    - sudo apt-get install -qq samtools
    - sudo apt-get install -qq bwa

install: "pip install -r requirements.txt"

script: "python ProDuSe/ProdusePipeline.py -c etc/produse_config.ini"
